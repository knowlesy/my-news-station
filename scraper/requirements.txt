# ═══════════════════════════════════════════════════════════════════
# Daily News Media Station — Python Dependencies
# Flexible pins for Python 3.14 / macOS ARM64 compatibility
# ═══════════════════════════════════════════════════════════════════

# ── Browser automation & stealth ─────────────────────────────────
playwright>=1.44.0
playwright-stealth>=1.0.6

# ── Content extraction ───────────────────────────────────────────
# trafilatura 2.x has relaxed lxml requirements + Python 3.14 support
trafilatura>=2.0.0
feedparser>=6.0.11
beautifulsoup4>=4.12.3
# lxml: let pip resolve the version compatible with trafilatura + Python 3.14
lxml>=4.9.4

# ── HTTP client (Gemini REST calls) ─────────────────────────────
requests>=2.32.0

# ── NLP / curation ───────────────────────────────────────────────
scikit-learn>=1.5.0

# ── EPUB generation ──────────────────────────────────────────────
ebooklib>=0.18

# ── Text-to-speech ───────────────────────────────────────────────
edge-tts>=6.1.12

# ── Anthropic SDK (claude_api backend) ───────────────────────────
anthropic>=0.116.0
